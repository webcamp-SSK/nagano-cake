<div class="p-5">
  <h3 class="ml-5 mini-title">注文履歴一覧</h3>
</div>

<div class="row mx-5">
  <table class="table mx-5 table-borderless">
    <thead class="thead-light">
      <tr>
        <th>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
        <tr>
          <td>
            <%= order.created_at.strftime('%Y/%m/%d　%H:%M') %>　リンク
          </td>
          <td>
            <%= order.customer.last_name + order.customer.first_name %>
          </td>
          <td>
            <%= order.order_details.count %> 修正要
          </td>
          <td>
            <%= order.status %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

