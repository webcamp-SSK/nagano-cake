<div class="p-5">
  <h3 class="mb-5 ml-5 mini-title">商品詳細</h3>
</div>

<div class="row">
  <div class="col-3 offset-1">
    <%= image_tag @item.image, size: "400x250" %>
  </div>
  <div class="col-6 offset-1">
    <table class="table table-borderless">
      <tbody>
        <tr>
          <td>商品名</td>
          <td><%= @item.name %></td>
        </tr>
        <tr>
          <td>商品説明</td>
          <td><%= @item.introduction %></td>
        </tr>
        <tr>
          <td>ジャンル</td>
          <td><%= @item.genre.name %></td>
        </tr>
        <tr>
          <td>税込価格<br>(税抜価格)</td>
          <td><%= @item.with_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</td>
        </tr>
        <tr>
          <td>販売ステータス</td>
          <td>
            <% if @item.is_active_i18n == "販売中" %>
              <b class="text-success"><%= @item.is_active_i18n %></b>
            <% else %>
              <b class="text-muted"><%= @item.is_active_i18n %></b>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
    <div class="row">
      <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success col-2 offset-3" %>
    </div>
  </div>
</div>
