<div class="p-5 mt-5 ml-5">
  <div class="ml-5 pl-5">
    <h3 class="ml-5 mt-5 mini-title">商品詳細</h3>
  </div>
</div>

<div class="row pt-5">
  <div class="col-3 offset-1">
    <%= image_tag @item.image, size: "400x250" %>
  </div>
  <div class="col-5">
    <table class="table table-borderless">
      <tbody>
        <tr>
          <td><h6>商品名</h6></td>
          <td><h5><%= @item.name %></h5></td>
        </tr>
        <tr>
          <td><h6>商品説明</h6></td>
          <td><h5><%= @item.introduction %></h5></td>
        </tr>
        <tr>
          <td><h6>ジャンル</h6></td>
          <td><h5><%= @item.genre.name %></h5></td>
        </tr>
        <tr>
          <td>税込価格<br>(税抜価格)</td>
          <td><h5><%= @item.with_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</h5></td>
        </tr>
        <tr>
          <td><h6>販売ステータス</h6></td>
          <td>
            <% if @item.is_active_i18n == "販売中" %>
              <h5><b class="text-success"><%= @item.is_active_i18n %></b></h5>
            <% else %>
              <h5><b class="text-muted"><%= @item.is_active_i18n %></b></h5>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
  </div>
</div>

<div class="row px-5 mt-4">
  <div class="col-2 offset-5">
    <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success btn-lg px-5" %>
  </div>
</div>
